using HuntTheWumpus.Models;

namespace UnitTest.ModelTests
{
    [TestClass]
    public class LoopStreamTest
    {
        //This model makes it so audio can be looped
        //Since this model extends WaveStream, I'm not going to test it
         
    }
}